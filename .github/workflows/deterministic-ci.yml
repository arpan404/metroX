name: Deterministic CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  server-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup uv
        uses: astral-sh/setup-uv@v4
        with:
          version: '0.8.0'
      - name: Setup Python
        run: uv python install 3.13
      - name: Install backend dependencies
        run: |
          cd apps/server
          uv sync --dev --frozen
      - name: Migration smoke check
        env:
          METROX_DATABASE_URL: sqlite:///./ci.db
        run: |
          cd apps/server
          uv run alembic upgrade head
      - name: Run deterministic backend tests
        env:
          METROX_DATABASE_URL: sqlite:///./ci.db
          METROX_API_KEY: ci-key
        run: |
          cd apps/server
          uv run pytest -q

  server-load-benchmark:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup uv
        uses: astral-sh/setup-uv@v4
        with:
          version: '0.8.0'
      - name: Setup Python
        run: uv python install 3.13
      - name: Install backend dependencies
        run: |
          cd apps/server
          uv sync --dev --frozen
      - name: Run deep-run load benchmark fixture
        env:
          METROX_DATABASE_URL: sqlite:///./ci-load.db
          METROX_API_KEY: ci-key
          METROX_ENABLE_LOAD: "1"
        run: |
          cd apps/server
          uv run pytest -q -m load

  client-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: |
          cd apps/client
          npm ci || npm install
      - name: Run frontend tests
        run: |
          cd apps/client
          npm run test -- --run
      - name: Build client
        run: |
          cd apps/client
          npm run build
