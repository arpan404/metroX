name: Nightly Live Evals

on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  nightly-live:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        provider: [litellm, openai_compatible]
    steps:
      - uses: actions/checkout@v4
      - name: Setup uv
        uses: astral-sh/setup-uv@v4
        with:
          version: '0.8.0'
      - name: Setup Python
        run: uv python install 3.13
      - name: Install backend dependencies
        run: |
          cd apps/server
          uv sync --dev --frozen
      - name: Run nightly live suite
        env:
          METROX_DATABASE_URL: sqlite:///./nightly.db
          METROX_API_KEY: ${{ secrets.METROX_API_KEY }}
          METROX_LIVE_PROVIDER_KEY: ${{ secrets.METROX_LIVE_PROVIDER_KEY }}
          METROX_NIGHTLY_PROVIDER: ${{ matrix.provider }}
          METROX_OPENAI_COMPAT_BASE_URL: ${{ secrets.METROX_OPENAI_COMPAT_BASE_URL }}
          METROX_OPENAI_COMPAT_API_KEY: ${{ secrets.METROX_OPENAI_COMPAT_API_KEY }}
          METROX_NIGHTLY_MODEL: ${{ secrets.METROX_NIGHTLY_MODEL }}
        run: |
          cd apps/server
          uv run pytest -q -m nightly
