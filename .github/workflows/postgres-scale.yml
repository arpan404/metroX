name: Postgres Scale

on:
  workflow_dispatch:
  schedule:
    - cron: '0 6 * * *'

jobs:
  postgres-scale:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: metrox
          POSTGRES_PASSWORD: metrox
          POSTGRES_DB: metrox_scale
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U metrox -d metrox_scale"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    steps:
      - uses: actions/checkout@v4
      - name: Setup uv
        uses: astral-sh/setup-uv@v4
        with:
          version: '0.8.0'
      - name: Setup Python
        run: uv python install 3.13
      - name: Install backend dependencies
        run: |
          cd apps/server
          uv sync --dev --frozen
      - name: Run Postgres 10k+ scale tests
        env:
          METROX_API_KEY: local-dev-key
          METROX_ENABLE_PG_SCALE: "1"
          METROX_POSTGRES_SCALE_URL: postgresql+psycopg://metrox:metrox@localhost:5432/metrox_scale
        run: |
          cd apps/server
          uv run pytest -q -m scale_pg
